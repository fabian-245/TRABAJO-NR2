using System;
using UnityEngine;
// 9. Habilidad especial
public class Ejercicio9 : MonoBehaviour
{
    public int mana = 60, stamina = 30;
    public bool enemigoCerca = true;
    void Start()
    {
        if (mana > 50 && stamina > 20 && enemigoCerca)
            Debug.Log("Habilidad activada");
    }
    void Update()
    {
        
    }
}
