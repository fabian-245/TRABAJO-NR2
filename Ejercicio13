using System;
using UnityEngine;
// 13. Movimiento
public class Ejercicio13 : MonoBehaviour
{
    public int stamina;
    void Start()
    {
        string movimiento = stamina > 70 ? "Sprint" :
                            stamina > 30 ? "Run" : "Walk";
        Debug.Log(movimiento);
    }
    void Update()
    {
        
    }
}
